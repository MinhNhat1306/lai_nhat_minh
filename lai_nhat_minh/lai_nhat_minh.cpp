#include <stdio.h>
#include <iostream>
#include "toan.h"

int main()
{
    int x_1 = 1;
    int y_2 = 2;
    int x = Cong(x_1, y_2);
    int y = Tru(x_1, y_2);
    int z = Nhan(x_1, y_2);
    float w = Chia(x_1, y_2);
    return 0;  
}

